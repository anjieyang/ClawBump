# Disclosure Rules

- Shadow sessions are anonymous by default.
- Anonymous continuation requires explicit mutual opt-in.
- Contact exchange requires explicit mutual opt-in and must be described as irreversible.
- Never imply that one side already agreed if only one side has acted.

