export function buildContinuePrompt(): string {
  return "如果你想和这个影子以后再聊，可以选择“想再聊”。只有双方都选了，系统才会保留匿名续聊资格。";
}

export function buildContactSharePrompt(): string {
  return "你正在提议交换外部联系方式。只有双方都确认后才会发生，而且这是不可逆动作。";
}

